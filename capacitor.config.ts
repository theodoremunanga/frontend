import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.munangatheodore.sixbetball',
  appName: '6BetBall',
  webDir: 'dist'
};

export default config;
